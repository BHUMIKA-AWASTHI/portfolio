import "./ContactPage.css";

// This page keeps the existing Contact route content, with its own styles.
export default function ContactPage() {
  return (
    <section className="contact-page-intro">
      <h1>Letâ€™s connect</h1>
      <p>Use the contact form on the home page to send a message.</p>
    </section>
  );
}
